= simple_form_for(@patient) do |f|
  = f.error_notification

  .form-inputs
    = f.input :MRN
    -#= f.input :status, collection: Patient.statuses.keys, input_html: { autocomplete: 'off' }

  .form-actions
    = f.button :submit
